{
    "id": "powx-n",
    "title": "Pow(x, n)",
    "description": "Implement pow(x, n), which calculates x raised to the power n (i.e., x^n).",
    "examples": [
        {
            "input": "x = 2.00000, n = 10",
            "output": "1024.00000"
        },
        {
            "input": "x = 2.10000, n = 3",
            "output": "9.26100"
        },
        {
            "input": "x = 2.00000, n = -2",
            "output": "0.25000",
            "explanation": "2^-2 = 1/2^2 = 1/4 = 0.25"
        }
    ],
    "constraints": [
        "-100.0 < x < 100.0",
        "-2^31 <= n <= 2^31 - 1",
        "n is an integer.",
        "Either x is not zero or n > 0.",
        "-10^4 <= x^n <= 10^4"
    ],
    "difficulty": "Medium",
    "topic": "Math & Geometry",
    "strategies": {
        "brute-force": {
            "id": "brute-force",
            "name": "Brute Force",
            "description": "Multiply x by itself n times. (Will TLE for large n)",
            "steps": [
                {
                    "id": 1,
                    "instruction": "Define the function my_pow that takes x and n.",
                    "placeholderCode": "def my_pow(x, n):",
                    "validationRegex": "^def\\s+my_pow\\(x,\\s*n\\):$",
                    "hint": "Use `def` to define the function.",
                    "solutionCode": "def my_pow(x, n):"
                },
                {
                    "id": 2,
                    "instruction": "Handle negative n.",
                    "placeholderCode": "    if n < 0:\n        x = 1 / x\n        n = -n",
                    "validationRegex": "^\\s+if\\s+n\\s*<\\s*0:\\s*x\\s*=\\s*1\\s*/\\s*x\\s*n\\s*=\\s*-n$",
                    "hint": "If n is negative, invert x and make n positive.",
                    "solutionCode": "    if n < 0:\n        x = 1 / x\n        n = -n"
                },
                {
                    "id": 3,
                    "instruction": "Loop n times.",
                    "placeholderCode": "    res = 1\n    for _ in range(n):\n        res *= x",
                    "validationRegex": "^\\s+res\\s*=\\s*1\\s*for\\s+_\\s+in\\s+range\\(n\\):\\s*res\\s*\\*=\\s*x$",
                    "hint": "Multiply x by itself n times.",
                    "solutionCode": "    res = 1\n    for _ in range(n):\n        res *= x"
                },
                {
                    "id": 4,
                    "instruction": "Return result.",
                    "placeholderCode": "    return res",
                    "validationRegex": "^\\s+return\\s+res$",
                    "hint": "Return the result.",
                    "solutionCode": "    return res"
                }
            ]
        },
        "optimal": {
            "id": "optimal",
            "name": "Optimal",
            "description": "Recursive approach with O(log n).",
            "steps": [
                {
                    "id": 1,
                    "instruction": "Define the function my_pow that takes x and n.",
                    "placeholderCode": "def my_pow(x, n):",
                    "validationRegex": "^def\\s+my_pow\\(x,\\s*n\\):$",
                    "hint": "Use `def` to define the function.",
                    "solutionCode": "def my_pow(x, n):"
                },
                {
                    "id": 2,
                    "instruction": "Define a helper function that takes x and n.",
                    "placeholderCode": "    def helper(x, n):",
                    "validationRegex": "^\\s+def\\s+helper\\(x,\\s*n\\):$",
                    "hint": "Define a recursive helper function.",
                    "solutionCode": "    def helper(x, n):"
                },
                {
                    "id": 3,
                    "instruction": "Base case: if x is 0, return 0.",
                    "placeholderCode": "        if x == 0: return 0",
                    "validationRegex": "^\\s+if\\s+x\\s*==\\s*0:\\s*return\\s+0$",
                    "hint": "Handle the base case where x is 0.",
                    "solutionCode": "        if x == 0: return 0"
                },
                {
                    "id": 4,
                    "instruction": "Base case: if n is 0, return 1.",
                    "placeholderCode": "        if n == 0: return 1",
                    "validationRegex": "^\\s+if\\s+n\\s*==\\s*0:\\s*return\\s+1$",
                    "hint": "Handle the base case where n is 0.",
                    "solutionCode": "        if n == 0: return 1"
                },
                {
                    "id": 5,
                    "instruction": "Recursively call the helper with n divided by 2.",
                    "placeholderCode": "        res = helper(x, n // 2)",
                    "validationRegex": "^\\s+res\\s*=\\s*helper\\(x,\\s*n\\s*//\\s*2\\)$",
                    "hint": "Calculate the result for n // 2.",
                    "solutionCode": "        res = helper(x, n // 2)"
                },
                {
                    "id": 6,
                    "instruction": "Square the result.",
                    "placeholderCode": "        res = res * res",
                    "validationRegex": "^\\s+res\\s*=\\s*res\\s*\\*\\s*res$",
                    "hint": "Multiply the result by itself.",
                    "solutionCode": "        res = res * res"
                },
                {
                    "id": 7,
                    "instruction": "If n is odd, multiply by x, otherwise return res.",
                    "placeholderCode": "        return x * res if n % 2 else res",
                    "validationRegex": "^\\s+return\\s+x\\s*\\*\\s*res\\s+if\\s+n\\s*%\\s*2\\s+else\\s+res$",
                    "hint": "If n is odd, multiply by x one more time.",
                    "solutionCode": "        return x * res if n % 2 else res"
                },
                {
                    "id": 8,
                    "instruction": "Call helper with absolute value of n and handle negative exponent.",
                    "placeholderCode": "    res = helper(x, abs(n))\n    return res if n >= 0 else 1 / res",
                    "validationRegex": "^\\s+res\\s*=\\s*helper\\(x,\\s*abs\\(n\\)\\)\\s*return\\s+res\\s+if\\s+n\\s*>=\\s*0\\s+else\\s+1\\s*/\\s*res$",
                    "hint": "Calculate power for absolute value of n. If n was negative, return reciprocal.",
                    "solutionCode": "    res = helper(x, abs(n))\n    return res if n >= 0 else 1 / res"
                }
            ]
        }
    }
}