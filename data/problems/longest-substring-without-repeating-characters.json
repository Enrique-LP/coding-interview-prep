{
    "id": "longest-substring-without-repeating-characters",
    "title": "Longest Substring Without Repeating Characters",
    "description": "Given a string s, find the length of the longest substring without repeating characters.",
    "examples": [
        {
            "input": "s = \"abcabcbb\"",
            "output": "3",
            "explanation": "The answer is \"abc\", with the length of 3."
        },
        {
            "input": "s = \"bbbbb\"",
            "output": "1",
            "explanation": "The answer is \"b\", with the length of 1."
        }
    ],
    "constraints": [
        "0 <= s.length <= 5 * 10^4",
        "s consists of English letters, digits, symbols and spaces."
    ],
    "difficulty": "Medium",
    "topic": "Sliding Window",
    "strategies": {
        "brute-force": {
            "id": "brute-force",
            "name": "Nested Loops",
            "description": "Check every substring for duplicates.",
            "steps": [
                {
                    "id": 1,
                    "instruction": "Define the function length_of_longest_substring that takes a string s.",
                    "placeholderCode": "def length_of_longest_substring(s):",
                    "validationRegex": "^def\\s+length_of_longest_substring\\(s\\):$",
                    "hint": "Use `def` to define the function.",
                    "solutionCode": "def length_of_longest_substring(s):"
                },
                {
                    "id": 2,
                    "instruction": "Initialize max_len to 0.",
                    "placeholderCode": "    max_len = 0",
                    "validationRegex": "^\\s+max_len\\s*=\\s*0$",
                    "hint": "Initialize `max_len`.",
                    "solutionCode": "    max_len = 0"
                },
                {
                    "id": 3,
                    "instruction": "Iterate through all possible substrings.",
                    "placeholderCode": "    for i in range(len(s)):\n        for j in range(i, len(s)):",
                    "validationRegex": "^\\s+for\\s+i\\s+in\\s+range\\(len\\(s\\)\\):\\s*for\\s+j\\s+in\\s+range\\(i,\\s*len\\(s\\)\\):$",
                    "hint": "Use nested loops to define substrings `s[i:j+1]`.",
                    "solutionCode": "    for i in range(len(s)):\n        for j in range(i, len(s)):"
                },
                {
                    "id": 4,
                    "instruction": "Check if substring has unique characters and update max_len.",
                    "placeholderCode": "            substring = s[i:j+1]\n            if len(set(substring)) == len(substring):\n                max_len = max(max_len, len(substring))",
                    "validationRegex": "^\\s+substring\\s*=\\s*s\\[i:j\\+1\\]\\s*if\\s+len\\(set\\(substring\\)\\)\\s*==\\s*len\\(substring\\):\\s*max_len\\s*=\\s*max\\(max_len,\\s*len\\(substring\\)\\)$",
                    "hint": "Convert substring to set to check for duplicates.",
                    "solutionCode": "            substring = s[i:j+1]\n            if len(set(substring)) == len(substring):\n                max_len = max(max_len, len(substring))"
                },
                {
                    "id": 5,
                    "instruction": "Return max_len.",
                    "placeholderCode": "    return max_len",
                    "validationRegex": "^\\s+return\\s+max_len$",
                    "hint": "Return the result.",
                    "solutionCode": "    return max_len"
                }
            ]
        },
        "optimal": {
            "id": "optimal",
            "name": "Sliding Window",
            "description": "Use a set and sliding window to track unique characters.",
            "steps": [
                {
                    "id": 1,
                    "instruction": "Define the function length_of_longest_substring that takes a string s.",
                    "placeholderCode": "def length_of_longest_substring(s):",
                    "validationRegex": "^def\\s+length_of_longest_substring\\(s\\):$",
                    "hint": "Use `def` to define the function.",
                    "solutionCode": "def length_of_longest_substring(s):"
                },
                {
                    "id": 2,
                    "instruction": "Initialize a set for characters, left pointer, and max_len.",
                    "placeholderCode": "    char_set = set()\n    l = 0\n    res = 0",
                    "validationRegex": "^\\s+char_set\\s*=\\s*set\\(\\)\\s*l\\s*=\\s*0\\s*res\\s*=\\s*0$",
                    "hint": "Initialize `char_set`, `l`, and `res`.",
                    "solutionCode": "    char_set = set()\n    l = 0\n    res = 0"
                },
                {
                    "id": 3,
                    "instruction": "Iterate with right pointer r through the string.",
                    "placeholderCode": "    for r in range(len(s)):",
                    "validationRegex": "^\\s+for\\s+r\\s+in\\s+range\\(len\\(s\\)\\):$",
                    "hint": "Iterate `r` from 0 to `len(s)`.",
                    "solutionCode": "    for r in range(len(s)):"
                },
                {
                    "id": 4,
                    "instruction": "If character is in set, remove from left until it's gone.",
                    "placeholderCode": "        while s[r] in char_set:\n            char_set.remove(s[l])\n            l += 1",
                    "validationRegex": "^\\s+while\\s+s\\[r\\]\\s+in\\s+char_set:\\s*char_set\\.remove\\(s\\[l\\]\\)\\s*l\\s*\\+=\\s*1$",
                    "hint": "While `s[r]` is in set, remove `s[l]` and increment `l`.\\n\\nExample:\\ns='abcabcbb'. Set={'a','b','c'}. r at index 3 ('a').\\n'a' is in set. Remove s[0] ('a'). l becomes 1. Set={'b','c'}.\\nNow 'a' can be added.",
                    "solutionCode": "        while s[r] in char_set:\n            char_set.remove(s[l])\n            l += 1"
                },
                {
                    "id": 5,
                    "instruction": "Add the current character to the set.",
                    "placeholderCode": "        char_set.add(s[r])",
                    "validationRegex": "^\\s+char_set\\.add\\(s\\[r\\]\\)$",
                    "hint": "Add `s[r]` to `char_set`.",
                    "solutionCode": "        char_set.add(s[r])"
                },
                {
                    "id": 6,
                    "instruction": "Update the result with the window size.",
                    "placeholderCode": "        res = max(res, r - l + 1)",
                    "validationRegex": "^\\s+res\\s*=\\s*max\\(res,\\s*r\\s*-\\s*l\\s*\\+\\s*1\\)$",
                    "hint": "Update `res` with `r - l + 1`.",
                    "solutionCode": "        res = max(res, r - l + 1)"
                },
                {
                    "id": 7,
                    "instruction": "Return the result.",
                    "placeholderCode": "    return res",
                    "validationRegex": "^\\s+return\\s+res$",
                    "hint": "Return the result.",
                    "solutionCode": "    return res"
                }
            ]
        }
    }
}