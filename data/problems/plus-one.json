{
    "id": "plus-one",
    "title": "Plus One",
    "description": "You are given a large integer represented as an integer array digits, where each digits[i] is the ith digit of the integer. The digits are ordered from most significant to least significant in left-to-right order. The large integer does not contain any leading 0's. Increment the large integer by one and return the resulting array of digits.",
    "examples": [
        {
            "input": "digits = [1,2,3]",
            "output": "[1,2,4]",
            "explanation": "The array represents the integer 123. Incrementing by one gives 123 + 1 = 124. Thus, the result should be [1,2,4]."
        },
        {
            "input": "digits = [9]",
            "output": "[1,0]",
            "explanation": "The array represents the integer 9. Incrementing by one gives 9 + 1 = 10. Thus, the result should be [1,0]."
        }
    ],
    "constraints": [
        "1 <= digits.length <= 100",
        "0 <= digits[i] <= 9",
        "digits does not contain any leading 0's."
    ],
    "difficulty": "Easy",
    "topic": "Math & Geometry",
    "strategies": {
        "brute-force": {
            "id": "brute-force",
            "name": "Brute Force",
            "description": "Convert to int, add 1, convert back.",
            "steps": [
                {
                    "id": 1,
                    "instruction": "Define the function plus_one that takes digits.",
                    "placeholderCode": "def plus_one(digits):",
                    "validationRegex": "^def\\s+plus_one\\(digits\\):$",
                    "hint": "Use `def` to define the function.",
                    "solutionCode": "def plus_one(digits):"
                },
                {
                    "id": 2,
                    "instruction": "Convert to int.",
                    "placeholderCode": "    num = int(\"\".join(map(str, digits)))",
                    "validationRegex": "^\\s+num\\s*=\\s*int\\(\"\"\\.join\\(map\\(str,\\s*digits\\)\\)\\)$",
                    "hint": "Convert the list of digits to a single integer.",
                    "solutionCode": "    num = int(\"\".join(map(str, digits)))"
                },
                {
                    "id": 3,
                    "instruction": "Add 1 and convert back.",
                    "placeholderCode": "    num += 1\n    return [int(x) for x in str(num)]",
                    "validationRegex": "^\\s+num\\s*\\+=\\s*1\\s*return\\s*\\[int\\(x\\)\\s+for\\s+x\\s+in\\s+str\\(num\\)\\]$",
                    "hint": "Add 1 to the integer and convert it back to a list of digits.",
                    "solutionCode": "    num += 1\n    return [int(x) for x in str(num)]"
                }
            ]
        },
        "optimal": {
            "id": "optimal",
            "name": "Optimal",
            "description": "Iterate backwards and handle carry.",
            "steps": [
                {
                    "id": 1,
                    "instruction": "Define the function plus_one that takes digits.",
                    "placeholderCode": "def plus_one(digits):",
                    "validationRegex": "^def\\s+plus_one\\(digits\\):$",
                    "hint": "Use `def` to define the function.",
                    "solutionCode": "def plus_one(digits):"
                },
                {
                    "id": 2,
                    "instruction": "Iterate backwards through the digits.",
                    "placeholderCode": "    for i in range(len(digits) - 1, -1, -1):",
                    "validationRegex": "^\\s+for\\s+i\\s+in\\s+range\\(len\\(digits\\)\\s*-\\s*1,\\s*-1,\\s*-1\\):$",
                    "hint": "Use a for loop to iterate from the last index to 0.",
                    "solutionCode": "    for i in range(len(digits) - 1, -1, -1):"
                },
                {
                    "id": 3,
                    "instruction": "Check if the current digit is less than 9.",
                    "placeholderCode": "        if digits[i] < 9:",
                    "validationRegex": "^\\s+if\\s+digits\\[i\\]\\s*<\\s*9:$",
                    "hint": "If digit is < 9, no carry is needed.",
                    "solutionCode": "        if digits[i] < 9:"
                },
                {
                    "id": 4,
                    "instruction": "Increment the current digit.",
                    "placeholderCode": "            digits[i] += 1",
                    "validationRegex": "^\\s+digits\\[i\\]\\s*\\+=\\s*1$",
                    "hint": "Add 1 to the digit.",
                    "solutionCode": "            digits[i] += 1"
                },
                {
                    "id": 5,
                    "instruction": "Return the digits array immediately.",
                    "placeholderCode": "            return digits",
                    "validationRegex": "^\\s+return\\s+digits$",
                    "hint": "Returning here handles the case where no further carry is needed.",
                    "solutionCode": "            return digits"
                },
                {
                    "id": 6,
                    "instruction": "Set the current digit to 0 if it was 9.",
                    "placeholderCode": "        digits[i] = 0",
                    "validationRegex": "^\\s+digits\\[i\\]\\s*=\\s*0$",
                    "hint": "If it was 9, it becomes 0 and the carry continues to the next iteration.",
                    "solutionCode": "        digits[i] = 0"
                },
                {
                    "id": 7,
                    "instruction": "Handle overflow (e.g., 999 -> 1000).",
                    "placeholderCode": "    return [1] + digits",
                    "validationRegex": "^\\s+return\\s*\\[1\\]\\s*\\+\\s*digits$",
                    "hint": "If the loop finishes, prepending 1 handles cases like 99 -> 100.",
                    "solutionCode": "    return [1] + digits"
                }
            ]
        }
    }
}