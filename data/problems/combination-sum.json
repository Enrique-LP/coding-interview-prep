{
    "id": "combination-sum",
    "title": "Combination Sum",
    "description": "Given an array of distinct integers candidates and a target integer target, return a list of all unique combinations of candidates where the chosen numbers sum to target. You may return the combinations in any order. The same number may be chosen from candidates an unlimited number of times. Two combinations are unique if the frequency of at least one of the chosen numbers is different.",
    "examples": [
        {
            "input": "candidates = [2,3,6,7], target = 7",
            "output": "[[2,2,3],[7]]"
        },
        {
            "input": "candidates = [2,3,5], target = 8",
            "output": "[[2,2,2,2],[2,3,3],[3,5]]"
        }
    ],
    "constraints": [
        "1 <= candidates.length <= 30",
        "2 <= candidates[i] <= 40",
        "All elements of candidates are distinct.",
        "1 <= target <= 40"
    ],
    "difficulty": "Medium",
    "topic": "Backtracking",
    "strategies": {
        "brute-force": {
            "id": "brute-force",
            "name": "Brute Force",
            "description": "DFS with decision to include same number again or move next.",
            "steps": [
                {
                    "id": 1,
                    "instruction": "Define the function combination_sum that takes candidates and target.",
                    "placeholderCode": "def combination_sum(candidates, target):",
                    "validationRegex": "^def\\s+combination_sum\\(candidates,\\s*target\\):$",
                    "hint": "Use `def` to define the function.",
                    "solutionCode": "def combination_sum(candidates, target):"
                },
                {
                    "id": 2,
                    "instruction": "Initialize result list.",
                    "placeholderCode": "    res = []",
                    "validationRegex": "^\\s+res\\s*=\\s*\\[\\]$",
                    "hint": "Initialize a list to store the valid combinations.",
                    "solutionCode": "    res = []"
                },
                {
                    "id": 3,
                    "instruction": "Define dfs function taking index i, current combination cur, and current total.",
                    "placeholderCode": "    def dfs(i, cur, total):\n        if total == target:\n            res.append(cur.copy())\n            return",
                    "validationRegex": "^\\s+def\\s+dfs\\(i,\\s*cur,\\s*total\\):\\s*if\\s+total\\s*==\\s*target:\\s*res\\.append\\(cur\\.copy\\(\\)\\)\\s*return$",
                    "hint": "Define DFS. If total equals target, add copy of current combination to results.",
                    "solutionCode": "    def dfs(i, cur, total):\n        if total == target:\n            res.append(cur.copy())\n            return"
                },
                {
                    "id": 4,
                    "instruction": "Base cases for stopping recursion.",
                    "placeholderCode": "        if i >= len(candidates) or total > target:\n            return",
                    "validationRegex": "^\\s+if\\s+i\\s*>=\\s+len\\(candidates\\)\\s+or\\s+total\\s*>\\s+target:\\s*return$",
                    "hint": "Stop if index out of bounds or total exceeds target.",
                    "solutionCode": "        if i >= len(candidates) or total > target:\n            return"
                },
                {
                    "id": 5,
                    "instruction": "Include candidates[i] and recurse (stay at i).",
                    "placeholderCode": "        cur.append(candidates[i])\n        dfs(i, cur, total + candidates[i])",
                    "validationRegex": "^\\s+cur\\.append\\(candidates\\[i\\]\\)\\s*dfs\\(i,\\s*cur,\\s*total\\s*\\+\\s*candidates\\[i\\]\\)$",
                    "hint": "Add current candidate and recurse with same index (allowing reuse).\\n\\nExample:\\nTarget=7, Candidates=[2, 3].\\nStart: 0. Add 2 -> Total=2. Recurse (index 0).\\nAdd 2 -> Total=4. Recurse (index 0).\\nAdd 2 -> Total=6. Recurse (index 0).\\nAdd 2 -> Total=8 (>7). Backtrack.",
                    "solutionCode": "        cur.append(candidates[i])\n        dfs(i, cur, total + candidates[i])"
                },
                {
                    "id": 6,
                    "instruction": "Exclude candidates[i] and recurse (move to i + 1).",
                    "placeholderCode": "        cur.pop()\n        dfs(i + 1, cur, total)",
                    "validationRegex": "^\\s+cur\\.pop\\(\\)\\s*dfs\\(i\\s*\\+\\s*1\\s*,\\s*cur,\\s*total\\)$",
                    "hint": "Backtrack (remove last element) and recurse with next index.",
                    "solutionCode": "        cur.pop()\n        dfs(i + 1, cur, total)"
                },
                {
                    "id": 7,
                    "instruction": "Call dfs and return result.",
                    "placeholderCode": "    dfs(0, [], 0)\n    return res",
                    "validationRegex": "^\\s+dfs\\(0,\\s*\\[\\],\\s*0\\)\\s*return\\s+res$",
                    "hint": "Start DFS from index 0 with empty list and 0 total.",
                    "solutionCode": "    dfs(0, [], 0)\n    return res"
                }
            ]
        },
        "optimal": {
            "id": "optimal",
            "name": "Optimal",
            "description": "DFS with backtracking exploring each candidate.",
            "steps": [
                {
                    "id": 1,
                    "instruction": "Define the function combination_sum that takes candidates and target.",
                    "placeholderCode": "def combination_sum(candidates, target):",
                    "validationRegex": "^def\\s+combination_sum\\(candidates,\\s*target\\):$",
                    "hint": "Use `def` to define the function.",
                    "solutionCode": "def combination_sum(candidates, target):"
                },
                {
                    "id": 2,
                    "instruction": "Initialize result list.",
                    "placeholderCode": "    res = []",
                    "validationRegex": "^\\s+res\\s*=\\s*\\[\\]$",
                    "hint": "Initialize a list to store the valid combinations.",
                    "solutionCode": "    res = []"
                },
                {
                    "id": 3,
                    "instruction": "Define the inner dfs function taking current index, combination, and total.",
                    "placeholderCode": "    def dfs(i, cur, total):",
                    "validationRegex": "^\\s+def\\s+dfs\\(i,\\s*cur,\\s*total\\):$",
                    "hint": "The function explores branches of the decision tree.",
                    "solutionCode": "    def dfs(i, cur, total):"
                },
                {
                    "id": 4,
                    "instruction": "Add a base case to check if the current total matches the target.",
                    "placeholderCode": "        if total == target:",
                    "validationRegex": "^\\s+if\\s+total\\s*==\\s*target:$",
                    "hint": "When this condition is true, we have found a valid combination.",
                    "solutionCode": "        if total == target:"
                },
                {
                    "id": 5,
                    "instruction": "Append a copy of the current combination to the result list.",
                    "placeholderCode": "            res.append(cur.copy())",
                    "validationRegex": "^\\s+res\\.append\\(cur\\.copy\\(\\)\\)$",
                    "hint": "Use .copy() to save the current state of the combination list.",
                    "solutionCode": "            res.append(cur.copy())"
                },
                {
                    "id": 6,
                    "instruction": "Return from the function after adding the combination to results.",
                    "placeholderCode": "            return",
                    "validationRegex": "^\\s*return$",
                    "hint": "Stop searching this branch as we already reached the target.",
                    "solutionCode": "            return"
                },
                {
                    "id": 7,
                    "instruction": "Base cases for stopping recursion if index or total out of bounds.",
                    "placeholderCode": "        if i >= len(candidates) or total > target:\n            return",
                    "validationRegex": "^\\s+if\\s+i\\s*>=\\s+len\\(candidates\\)\\s+or\\s+total\\s*>\\s+target:\\s*return$",
                    "hint": "Return if we run out of candidates or exceed the target sum.",
                    "solutionCode": "        if i >= len(candidates) or total > target:\n            return"
                },
                {
                    "id": 8,
                    "instruction": "Include candidates[i] and recurse allowing the same index for reuse.",
                    "placeholderCode": "        cur.append(candidates[i])\n        dfs(i, cur, total + candidates[i])",
                    "validationRegex": "^\\s+cur\\.append\\(candidates\\[i\\]\\)\\s*dfs\\(i,\\s*cur,\\s*total\\s*\\+\\s*candidates\\[i\\]\\)$",
                    "hint": "Include the current candidate and continue searching from the same index.",
                    "solutionCode": "        cur.append(candidates[i])\n        dfs(i, cur, total + candidates[i])"
                },
                {
                    "id": 9,
                    "instruction": "Exclude candidates[i] (backtrack) and move to the next candidate.",
                    "placeholderCode": "        cur.pop()\n        dfs(i + 1, cur, total)",
                    "validationRegex": "^\\s+cur\\.pop\\(\\)\\s*dfs\\(i\\s*\\+\\s*1,\\s*cur,\\s*total\\)$",
                    "hint": "Remove the last added candidate and explore branches starting from the next index.",
                    "solutionCode": "        cur.pop()\n        dfs(i + 1, cur, total)"
                },
                {
                    "id": 10,
                    "instruction": "Initiate the DFS and return the final results.",
                    "placeholderCode": "    dfs(0, [], 0)\n    return res",
                    "validationRegex": "^\\s+dfs\\(0,\\s*\\[\\],\\s*0\\)\\s*return\\s+res$",
                    "hint": "Start the recursion from index 0 with an empty combination and zero sum.",
                    "solutionCode": "    dfs(0, [], 0)\n    return res"
                }
            ]
        }
    }
}